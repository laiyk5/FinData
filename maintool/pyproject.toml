[project]
name = "maintool"
version = "0.1.0"
description = "Maintenance tooling for the FinData repository."
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[dependency-groups]
dev = []

[tool.uv]
package = false
